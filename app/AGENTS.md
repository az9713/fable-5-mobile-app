# Expo HAS CHANGED

This project runs Expo SDK 54 (downgraded from 57 for Expo Go compatibility — see git
history). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any code.
